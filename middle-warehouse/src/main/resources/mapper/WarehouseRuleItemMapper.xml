<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2017 杭州端点网络科技有限公司
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="WarehouseRuleItem">
    <resultMap id="WarehouseRuleItemMap" type="WarehouseRuleItem">
        <id column="id" property="id"/>
        <result column="rule_id" property="ruleId"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="name" property="name"/>
        <result column="priority" property="priority"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        pousheng_warehouse_rule_items
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        rule_id, warehouse_id, name, priority, created_at, updated_at
    </sql>

    <sql id="vals">
        #{ruleId}, #{warehouseId}, #{name}, #{priority}, now(), now()
    </sql>

    <sql id="queryCondition">
        
        <if test="ruleId != null">and `rule_id` = #{ruleId}</if>
        
        <if test="warehouseId != null">and `warehouse_id` = #{warehouseId}</if>
        
        <if test="name != null">and `name` = #{name}</if>
        
        <if test="priority != null">and `priority` = #{priority}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="WarehouseRuleItem" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="WarehouseRuleItemMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="WarehouseRuleItemMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <select id="findByRuleId" parameterType="long" resultMap="WarehouseRuleItemMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE rule_id = #{ruleId}
    </select>

    <update id="update" parameterType="WarehouseRuleItem">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="ruleId != null">rule_id = #{ruleId},</if>
            <if test="warehouseId != null">warehouse_id = #{warehouseId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="priority != null">priority = #{priority},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByRuleId" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE rule_id = #{ruleId}
    </delete>

    <select id="paging" parameterType="list" resultMap="WarehouseRuleItemMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <where>
        <include refid="queryCondition"/>
        </where>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <where>
        <include refid="queryCondition"/>
        </where>
    </select>

</mapper>