
name: terminus/pousheng-middle # 能力key，格式为企业key/能力名称, 端点企业 key 为 terminus
version: 2.3.0 # 能力版本 参考https://semver.org/lang/zh-CN/
type: service # 能力类型, 现在有 service(服务), library(类库), business(业务应用), 默认是 service, 可以不填写, 但其他类型需主动声明
repository: git@git.terminus.io:pousheng/pousheng-middle.git # 该能力库的 Git 仓库地址

logo:  # logo, http地址
title: pousheng-middle # 能力中文名称
description:  宝胜中台系统 # 能力描述

homepage: http://yymiddle.pousheng.com/ # 项目主页，可以为空
consolepage: # dice控制台页面，可以为空

