<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="Refundtemplates">
    <resultMap id="RefundtemplatesMap" type="Refundtemplates">
        <id column="id" property="id"/>
        <result column="batch_code" property="batchCode"/>
        <result column="sale_code" property="saleCode"/>
        <result column="refund_type" property="refundType"/>
        <result column="ship_code" property="shipCode"/>
        <result column="sku_code" property="skuCode"/>
        <result column="num" property="num"/>
        <result column="apply_status" property="applyStatus"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <sql id="tb">
        parana_refunds_templates
    </sql>

    <sql id="cols_all">
        id, batch_code, refund_type, sale_code, ship_code, sku_code, num, apply_status, created_at, updated_at 
    </sql>

    <sql id="cols_exclude_id">
        batch_code, refund_type, sale_code, ship_code, sku_code, num, apply_status, created_at, updated_at
    </sql>

    <update id="updateApplyStatus" parameterType="Refundtemplates">
        UPDATE
        <include refid="tb"/>
        SET updated_at=now(), apply_status = 1 
        WHERE id=#{id} 
    </update>

    <select id="getRefundTemplatesListBycode" parameterType="Refundtemplates" resultMap="RefundtemplatesMap">
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE batch_code = #{batchCode}
    </select>

    <insert id="creates" parameterType="list">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" separator="," item="r">
            (
            #{r.batchCode},
            #{r.refundType},
            #{r.saleCode},
            #{r.shipCode},
            #{r.skuCode},
            #{r.num},
            #{r.applyStatus},
            now(),
            now()

            )
        </foreach>
    </insert>

</mapper>