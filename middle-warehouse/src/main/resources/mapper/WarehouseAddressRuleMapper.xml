<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2017 杭州端点网络科技有限公司
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="WarehouseAddressRule">
    <resultMap id="WarehouseAddressRuleMap" type="WarehouseAddressRule">
        <id column="id" property="id"/>
        <result column="address_id" property="addressId"/>
        <result column="address_name" property="addressName"/>
        <result column="rule_id" property="ruleId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        pousheng_warehouse_address_rules
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        address_id, address_name,  rule_id, created_at, updated_at
    </sql>

    <sql id="vals">
        #{addressId}, #{addressName},  #{ruleId}, now(), now()
    </sql>

    <sql id="queryCondition">
        
        <if test="addressId != null">and `address_id` = #{addressId}</if>
        

        <if test="ruleId != null">and `rule_id` = #{ruleId}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="WarehouseAddressRule" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="WarehouseAddressRuleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByRuleId" parameterType="long" resultMap="WarehouseAddressRuleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE rule_id = #{ruleId}
    </select>

   <select id="findByIds" parameterType="list" resultMap="WarehouseAddressRuleMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="WarehouseAddressRule">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="addressId != null">address_id = #{addressId},</if>
            <if test="addressName != null">address_name = #{addressName},</if>
            <if test="ruleId != null">rule_id = #{ruleId},</if>
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByRuleId" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE rule_id = #{rule_id}
    </delete>

    <select id="paging" parameterType="list" resultMap="WarehouseAddressRuleMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <where>
        <include refid="queryCondition"/>
        </where>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <where>
        <include refid="queryCondition"/>
        </where>
    </select>

</mapper>